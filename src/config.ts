export namespace CONFIG {
    export const numLimit = 6;  // ランダムの数値の上限を定義する定数。
    export const sequentialValues = 3;  //3つ以上連続した値
    export const rows = 5;  //縦方向の値
    export const cols = 6;  //横方向の値
}
